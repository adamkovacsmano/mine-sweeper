class MineField {
  constructor(x, y, ones, twos, mines) {
    this.x = x;
    this.y = y;
    this.ones = ones;
    this.twos = twos;
    this.mines = mines;
  }

  loop() {
    // for (let i = 0; i < x; i++) {
    //   for (let j = 0; j < y; j++) {
    //     array[i][j] = field++;
    //     document.write(field);
    //   }
    //   document.write("</br>");
    // }
  }

  render() {}
}
